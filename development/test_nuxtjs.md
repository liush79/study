## My envrionment
  * Ubuntu 14.04 64-bit

#### Current nodejs version
    node -v
    => v0.10.25

## nodejs install
    curl -sL https://deb.nodesource.com/setup_8.x | sudo -E bash -
    suto apt-get install nodejs
    
#### Updated nodejs version
    node -v
    => v8.9.4
    
## Install vue-cli
    sudo npm install -g vue-cli
    
## Use starter template
    vue init nuxt-community/starter-template <project-name>
    cd <project-name>
    npm isntall

## Run
    npm run dev
* You can connect using a browser to 'http://localhost:3000'.


## AND You can see that the <project-name> is installed.
![installed_project](development/images/installed_hello.jpeg)


